# 
<span>
    #text
<span>

#
텍스트 수정시 발생하는 #text는 특정 태그를 지칭하는 것이 아닌 문자열 자체를 의미한다.
셀렉션에서 발생하는 anchorNode와 focusNode는 텍스트 노드를 지칭한다.
이 노드의 부모노드는 텍스를 감싸는 태그인 span 태그이다.

#
사용자의 텍스트 입력 및 수정은 dom -> state 단방향 흐름을 갖는다.
* 삽입
  state node의 내용도 일치되어야한다.
  * bold인 경우 bold 길이가 변경되는 처리가 필요하다.
