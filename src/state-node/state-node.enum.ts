export enum StateNodeType {
    ROOT = "root",
    PARAGRAPH = "paragraph",
    SPAN = "span",
}

export enum StateNodeDiff {
    INSERTED = "inserted",
    DELETED = "deleted",
    MODIFIED = "modified",
}
