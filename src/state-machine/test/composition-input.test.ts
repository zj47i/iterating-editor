import "@testing-library/jest-dom";
import { CompositionInputStateMachine } from "../composition-input.state-machine";

test("CompositionInputStateMachine", () => {
    CompositionInputStateMachine()
});
